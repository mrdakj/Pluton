What file manager should be able to do:
ls
cd
change name
create file
delete file

class Current_dir 
const string absolute_path;
const immer::vector<File> data;
methods:
immer::vector<File> ls();
Current_dir cd(const string path);
Current_dir change_name(const string name, const string new_name);

class File
const string ime;
const char type (d, r);
const unsigned size;
methods:
File change_name(const string);

How to destroy object before it goes out of the scope?
